__pycache__/
*.pyc
.venv/
venv/
.DS_Store
data/live/
# Markdown reports are committed so they can be read on a phone straight from
# GitHub. The full projection CSV is regenerable and stays out of history.
reports/*.csv
